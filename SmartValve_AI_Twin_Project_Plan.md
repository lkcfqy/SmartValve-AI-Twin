# SmartValve AI Twin 确定版项目计划

## 0. 文件状态

- 计划版本：2.2
- 软件版本：0.6.0
- 诊断模型：`valvedna-rules-0.3.0`
- 更新日期：2026-07-13
- 当前阶段：软件工程闭环、可信身份、TLS/SSO 参考部署与签名恢复链已实现；S2 自有硬件和 S3 企业阀门验证待完成
- 硬件预算上限：200 元

这份文件替代早期“六周从零搭建”草案，是后续执行的唯一主计划。

## 1. 最终判断

这套项目已经可以作为拜访青岛伟隆阀门、申请课题合作或实习的技术投名状，但正确定位必须是：

> 一套已完成软件工程闭环、公开真实台架验证和单节点安全容器验收，等待用一款伟隆阀门完成
> 产品校准的阀门—管网双层数字孪生工程试点。

不能说“已达到伟隆生产系统”或“现场准确率 87.2%”。拜访价值来自完整方法、真实证据、失败工况
披露和接企业数据的低风险路径，不来自夸大的完成度。

## 2. 一句话价值主张

为每台电动阀门保存一次健康全行程 ValveDNA，以后用阀位、电流和时间曲线识别摩擦、卡涩、
行程受阻与执行器异常，再由 WNTR 计算它对下游压力和流量的业务影响，最终输出可追溯的维护建议。

## 3. 为什么值得伟隆合作

项目把三件通常分散的事情连成一条证据链：

1. 出厂/投运基线：每台阀门形成可比较的动作指纹。
2. 状态诊断：异常被定位到行程区间，并给出透明物理证据。
3. 系统后果：单阀异常被转换为受影响节点与维护优先级。

对伟隆可落在四个低风险场景：出厂一致性、售后复测、维修前后对比、智慧水务维护排序。

## 4. 当前已完成的系统

### 4.1 诊断与水力闭环

- S0 确定性全行程仿真，覆盖正常、摩擦、局部卡涩、受阻、执行器退化和位置漂移。
- ValveDNA 基线/当前签名、行程分区残差、健康分、NE 107 风格状态和建议动作。
- `normal / diagnosed / needs_review` 显式决策状态。
- WNTR 1.5 PDD 管网计算；真实 WNTR 不可用时不会在正式健康探针中伪装就绪。
- 可用行程到水力损失的接口可替换；当前映射明确是示意，不冒充产品 Kv/Cv。

### 4.2 真实公开证据

| 来源 | 实现 | 结果 | 关键限制 |
|---|---|---:|---|
| Cranfield | 180 组、2 种运动、3 种负载、3 类状态 | 未见负载开发 CV 87.2% / 宏 F1 0.874 | 不是水阀 |
| Cranfield 最弱折 | trap、-40 kgf、30 组完整留出 | 33.3% | 证明域迁移尚未解决 |
| SKAB | 前 400 点固定拟合、后 745 点评测 | 指标由 API/界面展示 | 只验证过程异常 |

所有公开文件有原始 URL、许可、大小和 SHA-256；运行时再次与同步清单比对。

### 4.3 仿真评测

| 工件 | 参数组合 | 唯一可观测轨迹 | 宏 F1 | 正常误报率 |
|---|---:|---:|---:|---:|
| 开发/分组回归 | 3,000 | 2,100 | 0.953 | 0% |
| 规则冻结挑战 | 1,500 | 1,050 | 0.874 | 0% |

严重度为 0 时不同故障参数生成相同正常信号，因此不把 3,000 说成 3,000 个独立样本。当前
1.0 拒识指标只代表越界供电电压，不代表通用分布外检测。

### 4.4 后端与审计

- FastAPI v1、严格 Pydantic 输入、CSV 10 MiB/200,000 行门禁与质量拒收。
- 正式环境支持 OIDC/JWT 严格验签与读/诊断/审计 RBAC；API Key 仅保留为单租户兼容模式。
- OIDC 操作者只能来自签名声明，身份主体、签发方、权限与令牌 ID 摘要在结果哈希前入链。
- Run ID、Correlation ID、软件/模型版本、证据等级和限制贯穿响应与报告。
- 完整基线、当前轨迹、特征、签名、请求参数和结果落库。
- SQLite WAL 记录不可更新/删除，并通过前后记录 SHA-256 形成可校验哈希链。
- 审计验证按不可变插入顺序执行，不受 NTP/RTC 时钟回拨影响。
- SQLite 在线一致性快照、独立 HMAC 签名、完整性/审计锚校验和离线原子恢复。
- `/health/live`、真实数据库/WNTR `/health/ready`、Prometheus 文本 `/metrics`。
- 安全响应头、每 IP 速率限制、上传上限、错误关联 ID 和关联异常日志。

### 4.5 前端体验

- 资产总览、单阀诊断、管网影响、三源验证、系统与审计五个工作区。
- S0 水阀—水流动画和 S1 Cranfield 丝杠执行器动画分离，不用水流包装机电数据。
- 必须点击“执行并固化诊断”才写审计；只改参数会保持旧结果并提示待执行。
- “三源验证”的固定实测回放使用只读 API；浏览工作区不会暗中新增诊断记录。
- 规则 `confidence` 在界面显示为“证据强度 / 非统计校准概率”。
- 所有虚构资产使用 `DEMO-*`；企业上传只显示“S3 候选”。
- 桌面与 390px 移动端通过浏览器回归，Deploy 菜单隐藏，无应用级 JavaScript 错误。

### 4.6 正式单节点容器

- Chainguard Python 3.14 构建/无 shell 运行镜像均按 digest 固定；依赖有版本与 SHA-256 锁文件。
- API 与控制台以 UID/GID 65532 运行，根文件系统只读，运行环境不含 pip 和构建工具。
- 丢弃全部 Linux capabilities，启用 `no-new-privileges`、PID/CPU/内存上限。
- 端口默认只绑定 `127.0.0.1`，公开数据只读挂载，审计使用独立命名卷。
- 全新卷启动、密钥门禁、WNTR readiness、哈希链和真实浏览器已验收。
- SPDX 2.3 SBOM 为 100 包；Grype 原始 7 项 CPython 命中均保留，OpenVEX 逐项复核后适用项为 0。
- 运行卷和备份卷分离；备份签名密钥与 API Key 明确禁止复用。
- 提供 digest 固定的非 root Caddy TLS 网关，以及 OAuth2 Proxy 浏览器 SSO 叠加配置。

## 5. 目前尚未完成

以下任何一项都不能靠改界面文案“完成”：

1. S2：尚未采购并采集 200 元自有样机数据。
2. S3：没有伟隆阀门原始动作曲线、型号参数或工程师标签。
3. 没有产品 Kv/Cv 曲线，WNTR 阻力映射尚未标定。
4. 没有长期退化到失效数据，不能做 RUL。
5. 没有压力密封台架，不能声称真实内漏诊断。
6. 已有单节点签名恢复、OIDC/RBAC 与 TLS/SSO 参考部署，但尚未接入合作方真实身份源、外部密钥管理、多节点高可用、异地灾备和现场可靠性验收。
7. 90 秒演示视频、两页伟隆 Proposal 和实物样机尚未制作。

因此，当前软件不是“只能看看的 Demo”，但部署等级是单节点工程试点，不是工厂生产系统。

## 6. S2：200 元自有样机确定方案

目标不是模拟工业压力，而是证明自有采集链、可重复动作和同一 API 处理实测 CSV。

| 物料 | 目标价格 |
|---|---:|
| ESP32-C3 | 20–30 元 |
| MG996R 或 DS3218 舵机 | 25–40 元 |
| INA219 电流模块 | 5–8 元 |
| AS5600 角度编码器与磁铁 | 8–12 元 |
| 小球阀或输出盘 + 摩擦片 | 10–20 元 |
| 5V 大电流电源 | 20–25 元 |
| 支架、联轴器、线材、紧固件 | 25–35 元 |
| 备用与运费 | 15–25 元 |
| 总计 | 128–195 元 |

最低采样字段：`timestamp_s, command_pct, position_pct, motor_current_a, direction`。

验收门槛：

- 健康重复动作至少 20 次；关键特征变异系数目标不高于 10%。
- 正常、逐级摩擦、局部受阻、机械限位各至少 10 次。
- 原始串口日志、转换 CSV、实验参数、照片/视频和 SHA-256 一起归档。
- 所有堵转低压限流、短时、有人看护。
- 只有通过统一 CSV 门禁与 API 执行的结果才计入 S2。

## 7. S3：与伟隆的最小合作请求

第一次不索要“大量生产数据”，只申请一个四周、单型号、离线联合 PoC。

伟隆提供：

- 一个目标型号的非保密口径、执行器和测试条件；
- 20–50 次健康全开/全关曲线；
- 若允许，少量维修前后或人工异常曲线；
- Kv/Cv 或可替代的开度—流量/压差试验点；
- 一名技术人员两次评审：字段/工况校核与结果复核。

项目提供：

- 本地离线数据适配，不公开企业原始数据；
- 该型号健康基线、重复性、异常区间与误报分析；
- 产品开度到 WNTR 损失映射的初步拟合；
- 可解释 PDF 报告、审计证据和下一阶段建议。

联合 PoC 通过条件由双方事先冻结，至少包含正常重复性、跨负载误报率、维护前后可检测性和
工程师对建议动作的认可度。未冻结指标前不先写“准确率 95%”。

## 8. 接下来 30 天执行顺序

### P0：拜访材料（1–3 天）

- 制作两页伟隆定制 Proposal：问题、现有证据、四周 PoC、所需最小数据。
- 录制 90 秒主视频：受控执行 → 动画 → 证据 → 管网后果 → 审计/PDF。
- 准备 5 分钟技术备份：180 组分组验证、最弱折、哈希链、安全容器。
- 固定一次可恢复的演示镜像和空白审计卷备份。

### P1：S2 自有硬件（第 1–2 周）

- 采购、装配、固件、采样率验证。
- 建立 20 次健康重复基线和三类人工故障。
- 运行统一 CSV 门禁，生成首个真正 S2 报告。

### P2：科研增强（第 2–3 周）

- 对 Cranfield `trap/-40 kgf` 域偏移做特征/归一化消融。
- 保留未见工况外层评测，禁止在同一折反复调参后称为最终测试。
- 增加按运动形式和负载的适用域判定；不满足时输出 `needs_review`。
- 只有在独立校准完成后，才把启发式证据强度升级为统计校准概率。

### P3：S3 接入准备（第 3–4 周）

- 输出企业字段模板、脱敏方案、数据保留期和操作员权限表。
- 设计 SQLite → 企业数据库迁移接口，但不在没有并发/运维需求时堆叠组件。
- 准备产品 Kv/Cv 拟合 notebook/脚本与验收模板。

## 9. 拜访演示脚本

1. 首屏先说：所有 `DEMO-*` 是合成场景，当前最高证据为 S1。
2. 点击执行局部卡涩，展示阀内动画、异常区间、证据强度和建议动作。
3. 进入管网影响，解释 WNTR 只负责后果，不解释机械根因。
4. 切换 Cranfield，展示真实丝杠执行器动画，不出现水流或水阀措辞。
5. 在三源验证先展示 87.2%，主动点出 33.3% 最弱折和下一步域校准。
6. 在系统页展示 SHA-256 数据校验、审计链、API Key 和正式 PDF。
7. 最后提出四周单型号 PoC，而不是要求对方直接采购系统。

## 10. 面对技术追问的标准回答

“这是不是纯 Demo？”

> 不是纯前端演示。诊断经版本化 API、数据门禁、WNTR 和不可变审计执行，正式容器已验收；
> 但没有伟隆数据，所以它是工程试点而不是已投产系统。

“87.2% 是伟隆阀门准确率吗？”

> 不是。它是 Cranfield 单一机电执行器的未见负载开发交叉验证；最弱折只有 33.3%，正说明
> 产品部署前必须做型号和负载域校准。

“为什么还用规则？”

> 当前数据量与来源不足以支撑不透明大模型。规则能追溯证据并明确拒识；真实产品数据到位后，
> 再用冻结外层评测比较轻量模型是否真正增益。

“能否直接进生产？”

> 当前可在单节点受控环境试点。生产前还需企业数据库、集中身份、TLS/密钥管理、灾备、负载与
> 故障切换测试，以及产品本体校准和现场验收。

## 11. 投名状完成门槛

- [x] 可运行软件闭环和正式单节点容器
- [x] S0 分组回归与冻结挑战工件
- [x] S1 Cranfield 全 180 组验证与失败折披露
- [x] S1 SKAB 真实水循环验证
- [x] 严格数据来源/证据等级/限制说明
- [x] 完整不可变审计、PDF、健康探针与指标
- [x] 签名一致性备份、篡改检测与离线恢复工具
- [x] 依赖锁、安全扫描、SBOM 流程、CI、许可证和安全文档
- [x] 桌面/移动端浏览器验收
- [ ] 90 秒演示视频
- [ ] 两页伟隆定制 Proposal
- [ ] S2 自有硬件与首批真实采集数据
- [ ] S3 伟隆单型号联合验证

达到前两项未完成的“视频 + Proposal”后即可正式预约拜访；S2 样机能显著加分但不是发出合作
邀请的前置条件。S3 是合作目标，不可能要求在合作前先完成。

## 12. 不允许的表述

- “世界首创”
- “已达到 Emerson/Rotork 生产产品水平”
- “已完成伟隆产品验证”
- “87.2% 现场准确率”
- “通用 OOD 100%”
- “已实现泄漏诊断或 RUL”
- “通过 NAMUR、SIL、计量或网络安全认证”

允许的表述：

> 对标头部企业公开的阀门签名与资产健康方法，完成了可复现、可解释、可审计的低成本工程
> 试点，并用公开真实台架证明数据链可迁移；下一步希望用伟隆一个型号完成产品级联合校准。
